import ExamsPage from "../pages/exams/ExamsPage";
import { IRoute } from "../types/common";

const examRoutes: IRoute[] = [
  {
    path: "exams",
    element: ExamsPage,
  },
];

export default examRoutes;
